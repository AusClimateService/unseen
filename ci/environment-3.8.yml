name: unseen-test
channels:
  - conda-forge
dependencies:
  - python=3.8
  - geopandas
  - regionmask
  - xarray
  - dask-core
  - numpy
  - pytest
  - cftime
  - pip
  - pip:
    - codecov
    - pytest-cov
